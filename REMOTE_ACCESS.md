# Удаленный доступ к веб-интерфейсу ESP32

Веб-сервер ESP32 работает на порту **80** и доступен только в локальной сети. Для удаленного доступа нужно настроить проброс портов на роутере.

## Способ 1: Проброс портов (Port Forwarding) - Рекомендуется

### Шаг 1: Узнайте IP адрес ESP32

1. Откройте веб-интерфейс ESP32 в локальной сети: `http://192.168.x.x`
2. Перейдите в меню **"⚙️ Настройки"** → **"📡 WiFi"**
3. Найдите IP адрес в разделе "Информация о WiFi" (например, `192.168.1.100`)

Или посмотрите в Serial Monitor при загрузке ESP32:
```
WiFi подключен!
IP адрес: 192.168.1.100
```

### Шаг 2: Настройте статический IP для ESP32 (рекомендуется)

Чтобы IP адрес ESP32 не менялся при перезагрузке роутера:

1. Зайдите в настройки вашего роутера (обычно `192.168.1.1` или `192.168.0.1`)
2. Найдите раздел **"DHCP"** или **"Резервирование IP адресов"**
3. Добавьте ESP32 в список резервирования по MAC адресу
   - MAC адрес можно найти в веб-интерфейсе: **"⚙️ Настройки"** → **"📡 WiFi"** → **"Информация о WiFi"**

### Шаг 3: Настройте проброс портов на роутере

1. Зайдите в настройки роутера (обычно `192.168.1.1` или `192.168.0.1`)
2. Найдите раздел **"Виртуальные серверы"**, **"Port Forwarding"**, **"Проброс портов"** или **"NAT"**
3. Создайте новое правило:
   - **Имя**: ESP32-Kotel (любое)
   - **Внешний порт**: `8080` (или любой другой, например `80`, но 80 может быть занят)
   - **Внутренний IP**: IP адрес ESP32 (например, `192.168.1.100`)
   - **Внутренний порт**: `80`
   - **Протокол**: `TCP`
   - **Состояние**: `Включено`

### Шаг 4: Узнайте внешний IP адрес роутера

1. Зайдите на сайт: https://2ip.ru/ или https://myip.com/
2. Запишите ваш внешний IP адрес (например, `123.45.67.89`)

**Внимание**: Если у вас динамический IP, он может меняться. В этом случае используйте **DDNS** (см. Способ 2).

### Шаг 5: Доступ к веб-интерфейсу

Откройте в браузере:
```
http://ВАШ_ВНЕШНИЙ_IP:8080
```

Например:
```
http://123.45.67.89:8080
```

---

## Способ 2: Динамический DNS (DDNS) - Для динамического IP

Если ваш провайдер выдает динамический IP адрес, используйте DDNS сервис:

### Популярные DDNS сервисы:
- **No-IP** (https://www.noip.com/) - бесплатный
- **DuckDNS** (https://www.duckdns.org/) - бесплатный
- **Dynu** (https://www.dynu.com/) - бесплатный

### Настройка DDNS:

1. Зарегистрируйтесь на выбранном сервисе
2. Создайте доменное имя (например, `kotel.ddns.net`)
3. Настройте обновление IP в роутере:
   - Найдите раздел **"DDNS"** или **"Динамический DNS"** в настройках роутера
   - Введите данные от DDNS сервиса
   - Включите автоматическое обновление

4. Доступ к веб-интерфейсу:
```
http://kotel.ddns.net:8080
```

---

## Способ 3: VPN подключение - Самый безопасный

### Вариант A: VPN сервер на роутере

Если ваш роутер поддерживает VPN (OpenVPN, WireGuard):

1. Включите VPN сервер в настройках роутера
2. Скачайте конфигурационный файл VPN
3. Подключитесь к VPN с вашего устройства
4. Откройте веб-интерфейс ESP32 по локальному IP:
```
http://192.168.1.100
```

### Вариант B: VPN сервер на отдельном устройстве

Настройте VPN сервер на Raspberry Pi или другом устройстве в вашей сети.

---

## Способ 4: Облачные туннели (для тестирования)

Для быстрого тестирования можно использовать временные туннели:

### ngrok (https://ngrok.com/)
```bash
ngrok http 192.168.1.100:80
```

### Cloudflare Tunnel (https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)
Бесплатный и более безопасный вариант.

---

## Безопасность

⚠️ **ВАЖНО**: Веб-интерфейс ESP32 **не защищен паролем**!

### Рекомендации по безопасности:

1. **Используйте VPN** (Способ 3) - самый безопасный вариант
2. **Измените внешний порт** на нестандартный (например, `8080` вместо `80`)
3. **Ограничьте доступ по IP** в настройках роутера (если поддерживается)
4. **Используйте сложный пароль WiFi** для защиты локальной сети
5. **Регулярно обновляйте прошивку** ESP32

### Планируется добавить:
- Аутентификацию на веб-интерфейсе (логин/пароль)
- HTTPS поддержку (SSL сертификат)

---

## Проверка доступности

### Из локальной сети:
```
http://192.168.1.100
```

### Из интернета (после настройки проброса портов):
```
http://ВАШ_ВНЕШНИЙ_IP:8080
```

### Проверка через curl:
```bash
curl http://ВАШ_ВНЕШНИЙ_IP:8080/api/status
```

---

## Устранение проблем

### Проблема: Не могу подключиться из интернета

1. **Проверьте настройки роутера**:
   - Убедитесь, что проброс портов включен
   - Проверьте, что внешний и внутренний порты указаны правильно
   - Убедитесь, что IP адрес ESP32 правильный

2. **Проверьте файрвол**:
   - Отключите файрвол Windows/антивирус временно для проверки
   - Проверьте настройки файрвола роутера

3. **Проверьте доступность из локальной сети**:
   - Убедитесь, что веб-интерфейс работает по локальному IP
   - Проверьте, что ESP32 подключен к WiFi

4. **Проверьте внешний IP**:
   - Убедитесь, что вы используете правильный внешний IP адрес
   - Проверьте, что IP не изменился (если динамический)

### Проблема: IP адрес ESP32 меняется

**Решение**: Настройте резервирование IP адреса в настройках DHCP роутера (см. Шаг 2).

### Проблема: Роутер не поддерживает проброс портов

**Решение**: Используйте VPN (Способ 3) или облачные туннели (Способ 4).

---

## Примеры настройки для популярных роутеров

### TP-Link:
1. Зайдите в `192.168.0.1` или `192.168.1.1`
2. **Переадресация** → **Виртуальные серверы** → **Добавить новую**
3. Заполните форму и сохраните

### ASUS:
1. Зайдите в `192.168.1.1`
2. **Интернет** → **Проброс портов**
3. Включите "Включить проброс портов" и добавьте правило

### Zyxel:
1. Зайдите в `192.168.1.1`
2. **NAT** → **Port Forwarding**
3. Добавьте новое правило

### D-Link:
1. Зайдите в `192.168.0.1`
2. **Расширенные** → **Виртуальные серверы**
3. Добавьте новое правило

---

## Дополнительная информация

- **Порт веб-сервера**: 80 (HTTP)
- **Порт OTA**: 3232 (ArduinoOTA)
- **Протокол**: HTTP (без шифрования)

Для получения IP адреса ESP32 через MQTT, подпишитесь на топик:
```
kotel/device1/ip
```
