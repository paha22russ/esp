# Изменения версии 3.0.6

## 🔴 Критичные улучшения безопасности:

### 1. Защита от разгона без наддува ✅
- **Логика:** Близко к целевой температуре (в пределах 3°C) вентилятор отключается на 30 секунд
- **Проверка:** Если рост температуры остановился (< 0.5°C за 30 сек) - включаем вентилятор для догона
- **Если рост продолжается:** Котел разгоняется сам, вентилятор не нужен
- **Состояние:** `FAN_TEST` во время теста

### 2. Использование heatingTimeout ✅
- **Логика:** Если за указанное время (30 мин по умолчанию) температура не достигла уставки - предупреждение
- **Состояние:** `HEATING_TIMEOUT`
- **Условие:** Разница между текущей температурой и уставкой > 5°C

### 3. Использование maxTemp ✅
- **Логика:** Предупреждение при достижении максимальной температуры (75°C по умолчанию)
- **Состояние:** `HIGH_TEMP`
- **Действие:** Только предупреждение, не блокирует работу

## 🟡 Важные улучшения комфорта:

### 4. Обнаружение прогорания угля ✅
- **Логика:** Отслеживание падения температуры при работающем вентиляторе
- **Условие:** Падение температуры в течение 10 минут
- **Состояние:** `COAL_BURNED`
- **Действие:** Предупреждение пользователю

### 5. Предупреждение о низкой температуре обратки ✅
- **Логика:** Если обратка < 40°C при работающем насосе - показываем значок ⚠️
- **Веб-интерфейс:** Маленький значок рядом с температурой обратки
- **API:** Поле `lowReturnTemp` в `/api/status`

### 6. Использование инерции системы ✅
- **Логика:** После выключения вентилятора насос продолжает работать по инерции
- **Условия:** 
  - Температура > `inertiaTemp` (55°C)
  - Время < `inertiaTime` (10 минут)
- **Цель:** Использовать остаточное тепло в системе

## 🔧 Улучшения логики насоса:

### 7. Новая логика работы насоса ✅
- **Если уличная температура < 0°C:** Насос работает постоянно
- **Если датчик уличной температуры в ошибке:** Насос работает постоянно
- **Если уличная температура >= 0°C:** Насос работает от 45°C (minTemp)
- **Если датчик подачи в ошибке:** Насос работает постоянно

### 8. Защита от застоя насоса ✅
- **Логика:** Если насос простаивает > 30 минут - включается на 2 минуты
- **Цель:** Предотвращение застоя и замерзания
- **Действие:** Автоматическое периодическое включение

## 📊 Новые состояния системы:

- `FAN_TEST` - тест вентилятора (отключение на 30 сек)
- `HEATING_TIMEOUT` - долгий разогрев
- `COAL_BURNED` - возможно уголь прогорел
- `HIGH_TEMP` - высокая температура (предупреждение)
- `OVERHEAT` - перегрев (уже было)
- `HEATING` - нагрев (уже было)
- `IDLE` - покой (уже было)

## 🔌 API изменения:

Добавлены поля в `/api/status`:
- `lowReturnTemp` (boolean) - низкая температура обратки
- `coalBurned` (boolean) - уголь прогорел

## 🎨 Веб-интерфейс:

- Добавлен значок ⚠️ рядом с температурой обратки при низкой температуре
- Анимация пульсации для значка предупреждения
- Оранжевый цвет (#F39C12) для значка

## ⚙️ Параметры (теперь используются):

- ✅ `heatingTimeout` - таймаут разогрева
- ✅ `inertiaTemp` - температура инерции
- ✅ `inertiaTime` - время инерции
- ✅ `maxTemp` - максимальная температура (предупреждение)

## 📝 Технические детали:

### Защита от разгона:
```cpp
FAN_TEST_TEMP_THRESHOLD = 3.0°C  // Порог близости к уставке
FAN_TEST_DURATION = 30 секунд    // Длительность теста
```

### Обнаружение прогорания:
```cpp
COAL_BURNED_CHECK_TIME = 10 минут  // Время падения температуры
```

### Защита от застоя:
```cpp
PUMP_ANTI_STAGNATION_INTERVAL = 30 минут  // Интервал проверки
PUMP_ANTI_STAGNATION_DURATION = 2 минуты   // Время работы
```

## 🚀 Готово к тестированию!

Все функции реализованы и готовы к загрузке на устройство.
