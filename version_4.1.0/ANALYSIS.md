# Анализ логики управления котлом v3.0.5

## Что реализовано:

### ✅ Базовые функции:
1. **Автоматическое управление вентилятором**
   - Включается при: `supplyTemp < (setpoint - hysteresis)`
   - Выключается при: `supplyTemp >= (setpoint + hysteresis)`
   - Состояние: `HEATING` / `IDLE`

2. **Автоматическое управление насосом**
   - Включается если: вентилятор работает ИЛИ `supplyTemp > minTemp`
   - Работает для циркуляции даже без вентилятора

3. **Защита от перегрева**
   - При `supplyTemp >= overheatTemp` (77°C) вентилятор выключается
   - Состояние: `OVERHEAT`

4. **Подброс угля**
   - Выключает вентилятор на 10 минут
   - Сохраняет предыдущее состояние вентилятора

### ⚠️ Параметры есть, но НЕ используются:

1. **`heatingTimeout` (30 мин)** - таймаут разогрева
   - Есть в настройках, но логика не реализована
   - Нужно: если за 30 минут не достигнута уставка - предупреждение/остановка

2. **`inertiaTemp` (55°C) и `inertiaTime` (10 мин)** - инерция системы
   - Есть в настройках, но логика не реализована
   - Нужно: после выключения вентилятора продолжать насос пока temp > inertiaTemp или время < inertiaTime

3. **`maxTemp` (75°C)** - максимальная температура
   - Есть в настройках, но не используется
   - Нужно: предупреждение при достижении

### ❌ Отсутствует:

1. **Обнаружение прогорания угля**
   - Нет логики определения когда уголь прогорел
   - Признаки: температура падает даже с работающим вентилятором
   - Нужно: отслеживать тренд температуры при работающем вентиляторе

2. **Защита от разгона без наддува**
   - Нет защиты от быстрого роста температуры без вентилятора
   - Признаки: температура растет >2°C/мин без вентилятора
   - Нужно: принудительно включать вентилятор при быстром росте

3. **Защита от закипания**
   - Нет защиты от закипания в системе
   - Нужно: контроль разницы температур подача-обратка

4. **Предупреждение о низкой температуре обратки**
   - Нет контроля минимальной температуры обратки
   - Нужно: предупреждение если обратка < 40°C при работающем насосе

5. **Защита от застоя**
   - Нет защиты от длительной работы без циркуляции
   - Нужно: минимальное время работы насоса после включения

6. **Умный старт**
   - Нет логики плавного запуска
   - Нужно: постепенное включение вентилятора при холодном старте

## Рекомендации по улучшению безопасности и комфорта:

### 🔴 Критичные (безопасность):
1. **Защита от разгона без наддува** - принудительное включение вентилятора
2. **Защита от закипания** - контроль разницы температур
3. **Использование heatingTimeout** - предупреждение при долгом разогреве

### 🟡 Важные (комфорт):
4. **Использование инерции** - продолжение циркуляции после выключения вентилятора
5. **Обнаружение прогорания угля** - уведомление пользователя
6. **Предупреждение о низкой обратке** - защита от холодной обратки

### 🟢 Желательные (удобство):
7. **Умный старт** - плавный запуск
8. **Использование maxTemp** - предупреждение при достижении
9. **Защита от застоя** - минимальное время работы насоса
