# Структура веб-меню

## Общая структура интерфейса

Веб-интерфейс построен на принципах одностраничного приложения (SPA) с переключением между страницами через JavaScript.

---

## 🏠 Главная страница (mainPage)

**ID элемента**: `mainPage`  
**Отображается**: По умолчанию при загрузке

### Компоненты главной страницы

#### 1. Заголовок (header)
- **Заголовок**: "🔥 Котел"
- **Кнопка меню**: "☰" - открывает выпадающее меню
- **Строка статуса**:
  - Индикатор состояния (цветная точка)
  - Текстовое состояние системы
  - Статус системы (Включена/Выключена)
  - Статус WiFi

#### 2. Предупреждение о перегреве
**ID**: `overheatWarning`  
**Отображается**: При превышении температуры перегрева  
**Стиль**: Красное предупреждение

#### 3. Отображение температур

**Блок 1 - Основные температуры**:
- **Подача** (`supplyTemp`): Температура на выходе котла
- **Обратка** (`returnTemp`): Температура на входе котла

**Блок 2 - Дополнительные температуры**:
- **Котельная** (`boilerTemp`): Температура в котельной
- **Улица** (`outdoorTemp`): Температура на улице

**Формат отображения**: "XX.X°C" или "--" если нет данных

#### 4. Управление уставкой

**Элементы**:
- Текст: "Уставка температуры"
- Отображение уставки: `setpointValue` (крупный шрифт)
- Кнопки: "-" и "+" для изменения уставки

**Функция**: `changeSetpoint(delta)` - изменяет уставку на ±1°C

#### 5. Переключатель системы

**Элемент**: Чекбокс `systemEnabled`  
**Функция**: `toggleSystem()` - включает/выключает систему  
**Отображение**: "Включена" / "Выключена"

#### 6. Подброс угля

**Элементы**:
- Заголовок: "🔥 Подброс угля"
- Статус: `coalFeedingStatus` - описание режима
- Кнопка: `coalFeedingButton` - переключатель режима
- Таймер: `coalFeedingTimer` - отображает время работы (MM:SS)

**Функция**: `toggleCoalFeeding()` - переключает режим подброса угля

**Состояния кнопки**:
- Неактивен: "🔥 Запустить подброс угля" (синяя)
- Активен: "⏹ Остановить подброс угля" (оранжевая)

#### 7. Иконки устройств

**Вентилятор** (`fanIcon`):
- Иконка: 💨
- Функция: `toggleDevice('fan')`
- Состояния: `active` / `inactive` (CSS классы)

**Насос** (`pumpIcon`):
- Иконка: 💧
- Функция: `toggleDevice('pump')`
- Состояния: `active` / `inactive` (CSS классы)

---

## 📋 Выпадающее меню

**ID**: `dropdownMenu`  
**Функция открытия**: `toggleMenu()`

### Пункты меню

1. **⚙️ Режим Авто** → `showSettings('auto')`
2. **📡 MQTT** → `showSettings('mqtt')`
3. **📶 WiFi** → `showSettings('wifi')`
4. **🌡️ Датчики** → `showSettings('sensors')`
5. **🔌 Подключение** → `showSettings('pins')`
6. **ℹ️ Система** → `showSettings('system')`

**Закрытие**: При клике вне меню или при выборе пункта

---

## ⚙️ Страница настроек: Режим Авто

**ID**: `settingsAuto`  
**Функция загрузки**: `loadAutoSettings()`

### Группа 1: Температуры котла

1. **Уставка температуры котла** (`autoSetpoint`)
   - Диапазон: 40-80°C
   - Шаг: 1°C

2. **Минимальная температура котла** (`autoMinTemp`)
   - Диапазон: 40-80°C
   - Шаг: 1°C

3. **Максимальная температура котла** (`autoMaxTemp`)
   - Диапазон: 40-80°C
   - Шаг: 1°C

4. **Гистерезис температуры** (`autoHysteresis`)
   - Диапазон: 0.5-5°C
   - Шаг: 0.5°C

### Группа 2: Параметры инерции

1. **Температура ожидания инерции** (`autoInertiaTemp`)
   - Диапазон: 40-80°C
   - Шаг: 1°C

2. **Время ожидания инерции** (`autoInertiaTime`)
   - Диапазон: 5-30 минут
   - Шаг: 1 минута

### Группа 3: Параметры безопасности

1. **Температура перегрева** (`autoOverheatTemp`)
   - Диапазон: 70-85°C
   - Шаг: 1°C

2. **Таймаут разогрева** (`autoHeatingTimeout`)
   - Диапазон: 10-60 минут
   - Шаг: 1 минута

### Кнопки

- **💾 Сохранить** → `saveAutoSettings()`
- **🔄 Загрузить** → `loadAutoSettings()`

**API**: 
- GET `/api/settings/auto` - загрузка
- POST `/api/settings/auto` - сохранение

---

## 📡 Страница настроек: MQTT

**ID**: `settingsMqtt`  
**Функция загрузки**: `loadMqttSettings()`

### Группа 1: Основные настройки

1. **Включить MQTT** (`mqttEnabled`)
   - Тип: Чекбокс
   - По умолчанию: включен

2. **Система включена** (`mqttSystemEnabled`)
   - Тип: Чекбокс
   - Дублирует основной переключатель системы

3. **Адрес сервера** (`mqttServer`)
   - Тип: Текст
   - По умолчанию: "m5.wqtt.ru"

4. **Порт** (`mqttPort`)
   - Тип: Выпадающий список
   - Варианты: 1883, 8883, 5374, 5375, 5377

5. **Использовать TLS** (`mqttUseTLS`)
   - Тип: Чекбокс

### Группа 2: Аутентификация

1. **Логин** (`mqttUser`)
   - Тип: Текст
   - По умолчанию: "u_OLTTB0"

2. **Пароль** (`mqttPassword`)
   - Тип: Пароль (скрытый ввод)
   - По умолчанию: "XDzCIXr0"

### Группа 3: Топики

1. **Префикс топиков** (`mqttPrefix`)
   - Тип: Текст
   - По умолчанию: "kotel/device1"

### Группа 4: Интервалы публикации

1. **Интервал температур** (`mqttTempInterval`)
   - Диапазон: 5-60 секунд
   - Шаг: 1 секунда
   - По умолчанию: 10 секунд

2. **Интервал состояния** (`mqttStateInterval`)
   - Диапазон: 10-120 секунд
   - Шаг: 5 секунд
   - По умолчанию: 30 секунд

### Кнопки

- **💾 Сохранить** → `saveMqttSettings()`
- **🔄 Загрузить** → `loadMqttSettings()`
- **🔍 Тест подключения** → `testMqttConnection()`

**API**:
- GET `/api/settings/mqtt` - загрузка
- POST `/api/settings/mqtt` - сохранение
- POST `/api/mqtt/test` - тест подключения

---

## 📶 Страница настроек: WiFi

**ID**: `settingsWiFi` (не найден в коде, возможно используется другой ID)  
**Функция загрузки**: `loadWiFiInfo()`

### Группа 1: Информация о подключении

**Отображаемые данные** (только для чтения):
- **SSID**: Имя сети
- **IP адрес**: Текущий IP
- **RSSI**: Уровень сигнала (dBm)
- **MAC адрес**: MAC адрес ESP32

### Группа 2: Сканирование сетей

**Элементы**:
- Кнопка: **🔍 Сканировать сети** → `scanWiFiNetworks()`
- Результаты: `wifiNetworksList` - список найденных сетей

**Отображение сетей**:
- SSID (имя сети)
- Иконка замка (🔒) для защищенных сетей
- Тип шифрования (WPA2, WPA, и т.д.)
- Канал
- Уровень сигнала (RSSI) с цветовой индикацией:
  - Зеленый: > -50 dBm (отличный)
  - Желтый: -50 до -70 dBm (хороший)
  - Оранжевый: -70 до -85 dBm (слабый)
  - Красный: < -85 dBm (очень слабый)

### Кнопки

- **🔄 Сброс WiFi** → `resetWiFi()` - сброс настроек и перезагрузка

**API**:
- GET `/api/wifi/info` - информация о WiFi
- GET `/api/wifi/scan` - запуск сканирования
- GET `/api/wifi/scan/results` - получение результатов
- POST `/api/wifi/reset` - сброс настроек

---

## 🌡️ Страница настроек: Датчики

**ID**: `settingsSensors`  
**Функция загрузки**: `loadSensorMapping()`

### Группа 1: Сканирование датчиков

**Элементы**:
- Кнопка: **🔍 Сканировать датчики** → `scanSensors()`
- Список найденных датчиков: `sensorList`
  - Адрес датчика (OneWire адрес)
  - Текущая температура (если доступна)

### Группа 2: Привязка датчиков

**Выпадающие списки**:
1. **Датчик подачи** (`sensorSupply`)
2. **Датчик обратки** (`sensorReturn`)
3. **Датчик котельной** (`sensorBoiler`)
4. **Датчик улицы** (`sensorOutside`)

**Варианты**: Список найденных датчиков + "Не выбран"

### Кнопки

- **💾 Сохранить привязку** → `saveSensorMapping()`

**API**:
- POST `/api/sensors/scan` - сканирование датчиков
- GET `/api/sensors/mapping` - загрузка привязки
- POST `/api/sensors/mapping` - сохранение привязки

---

## 🔌 Страница настроек: Подключение

**ID**: `settingsPins`  
**Описание**: Информационная страница с инструкциями по подключению

### Содержимое

1. **Реле управления**
   - GPIO 16 → Реле вентилятора
   - GPIO 17 → Реле насоса
   - Описание подключения

2. **OLED дисплей (SSD1306 128x64, I2C)**
   - GPIO 21 → SDA
   - GPIO 22 → SCL
   - Питание: 3.3V или 5V
   - Адрес I2C: 0x3C

3. **Датчики температуры DS18B20**
   - Шина 1 (GPIO 4): Подача, Обратка
   - Шина 2 (GPIO 5): Котельная, Улица
   - Подтягивающий резистор 4.7 кОм

4. **Энкодер**
   - GPIO 18 → CLK
   - GPIO 19 → DT
   - GPIO 23 → SW (кнопка)
   - Питание: 3.3V или 5V

5. **Питание**
   - Требования к питанию для каждого компонента

6. **Схема подключения**
   - Текстовая схема всех соединений

7. **Рекомендации**
   - Советы по монтажу и подключению

**Функция**: Только для просмотра, без сохранения настроек

---

## ℹ️ Страница настроек: Система

**ID**: `settingsSystem`  
**Функция загрузки**: `loadSystemInfo()`

### Группа 1: Информация о системе

**Поля (только для чтения)**:
1. **Версия прошивки** (`firmwareVersion`)
   - Формат: "1.0.3"

2. **IP адрес** (`systemIP`)
   - Формат: "192.168.1.100"

3. **MAC адрес** (`systemMAC`)
   - Формат: "E4:65:B8:13:83:94"

4. **Время работы** (`systemUptime`)
   - Формат: "1ч 30м 45с"

5. **Свободная память** (`systemFreeMem`)
   - Формат: "250 KB"

### Группа 2: OTA Обновление

**Элементы**:
1. **Выбор файла** (`otaFile`)
   - Тип: File input
   - Принимает: `.bin` файлы

2. **Кнопка загрузки** (`otaUploadBtn`)
   - Текст: "📤 Загрузить прошивку"
   - Функция: `uploadOTA()`

3. **Статус загрузки** (`otaStatus`)
   - Отображает прогресс и результат

**Инструкция**:
- Описание процесса обновления
- Где найти файл прошивки

**API**:
- POST `/update` - загрузка прошивки (multipart/form-data)

### Кнопки

- **🔄 Перезагрузка** → `rebootDevice()`

**API**:
- GET `/api/system/info` - информация о системе
- POST `/api/system/reboot` - перезагрузка

---

## 🔄 JavaScript функции

### Навигация

#### `toggleMenu()`
Переключает видимость выпадающего меню.

#### `showMain()`
Показывает главную страницу, скрывает все настройки.

#### `showSettings(type)`
Показывает страницу настроек указанного типа.

**Параметры**:
- `'auto'` - Режим Авто
- `'mqtt'` - MQTT
- `'wifi'` - WiFi
- `'sensors'` - Датчики
- `'pins'` - Подключение
- `'system'` - Система

**Действия**:
- Скрывает главную страницу
- Показывает выбранную страницу настроек
- Закрывает меню
- Загружает данные для страницы (если нужно)

---

### Обновление данных

#### `updateData()`
Загружает текущий статус системы через API.

**Вызывается**: Каждые 5 секунд (настраивается)

**Обновляет**:
- Все температуры
- Уставку
- Состояние устройств (вентилятор, насос)
- Статус системы
- Статус WiFi
- Статус подброса угля

**API**: GET `/api/status`

---

### Управление уставкой

#### `changeSetpoint(delta)`
Изменяет уставку температуры.

**Параметры**:
- `delta`: -1 или +1 (изменение на 1°C)

**Действия**:
1. Читает текущее значение
2. Вычисляет новое значение (ограничение 40-80°C)
3. Обновляет отображение
4. Отправляет POST запрос на `/api/setpoint?value={newValue}`

---

### Управление системой

#### `toggleSystem()`
Включает/выключает систему.

**Действия**:
1. Читает состояние чекбокса
2. Отправляет POST на `/api/system/enable?enabled={0|1}`
3. Обновляет отображение

---

### Управление устройствами

#### `toggleDevice(device)`
Переключает состояние устройства.

**Параметры**:
- `device`: `'fan'` или `'pump'`

**Действия**:
1. Определяет текущее состояние
2. Отправляет POST на `/api/control?device={device}&state={0|1}`
3. Обновляет иконку (добавляет/убирает класс `active`)

---

### Подброс угля

#### `toggleCoalFeeding()`
Переключает режим подброса угля.

**Действия**:
1. Отправляет POST на `/api/coalFeeding`
2. Обновляет кнопку и статус
3. Запускает/останавливает таймер

#### `updateCoalFeedingStatus(active, remainingSeconds)`
Обновляет отображение статуса подброса угля.

**Параметры**:
- `active`: boolean - активен ли режим
- `remainingSeconds`: int - время работы в секундах

#### `startCoalFeedingTimer()`
Запускает таймер для отображения времени работы.

**Действия**: Периодически запрашивает статус и обновляет таймер.

---

### Загрузка настроек

#### `loadAutoSettings()`
Загружает настройки автоматического режима.

**API**: GET `/api/settings/auto`

#### `saveAutoSettings()`
Сохраняет настройки автоматического режима.

**API**: POST `/api/settings/auto`

#### `loadMqttSettings()`
Загружает настройки MQTT.

**API**: GET `/api/settings/mqtt`

#### `saveMqttSettings()`
Сохраняет настройки MQTT.

**API**: POST `/api/settings/mqtt`

#### `testMqttConnection()`
Тестирует подключение к MQTT брокеру.

**API**: POST `/api/mqtt/test`

#### `loadWiFiInfo()`
Загружает информацию о WiFi.

**API**: GET `/api/wifi/info`

#### `scanWiFiNetworks()`
Запускает сканирование WiFi сетей.

**Процесс**:
1. Отправляет GET на `/api/wifi/scan`
2. Периодически проверяет `/api/wifi/scan/results`
3. Отображает найденные сети

#### `displayWiFiScanResults(networks)`
Отображает результаты сканирования WiFi.

**Параметры**:
- `networks`: массив объектов с информацией о сетях

#### `loadSensorMapping()`
Загружает привязку датчиков.

**API**: GET `/api/sensors/mapping`

#### `scanSensors()`
Сканирует датчики температуры.

**API**: POST `/api/sensors/scan`

#### `saveSensorMapping()`
Сохраняет привязку датчиков.

**API**: POST `/api/sensors/mapping`

#### `loadSystemInfo()`
Загружает системную информацию.

**API**: GET `/api/system/info`

#### `uploadOTA()`
Загружает прошивку через веб-интерфейс.

**Процесс**:
1. Проверяет выбранный файл (.bin)
2. Создает FormData
3. Отправляет POST на `/update`
4. Отображает прогресс и результат

#### `rebootDevice()`
Перезагружает устройство.

**API**: POST `/api/system/reboot`

---

## 🎨 Стили и темы

### Темная тема (по умолчанию)

**CSS переменные**:
- `--bg-color`: #1A1A1A
- `--text-color`: #ECF0F1
- `--card-bg`: #2C2C2C
- `--border-color`: #404040
- `--theme-primary`: #5B9BD2
- `--theme-gradient`: linear-gradient(135deg, #2C3E50 0%, #34495E 100%)

**Применяется**: Автоматически при загрузке страницы

---

## 📱 Адаптивность

Интерфейс оптимизирован для мобильных устройств:
- Максимальная ширина: 420px
- Адаптивные шрифты: `clamp()`
- Touch-friendly кнопки
- Вертикальная прокрутка

---

## 🔄 Автообновление

**Интервал обновления данных**: 5 секунд

**Обновляемые элементы**:
- Все температуры
- Уставка
- Состояния устройств
- Статус системы
- Статус WiFi
- Статус подброса угля
- Таймер подброса угля

**Оптимизация**: Увеличен интервал до 5 секунд для снижения нагрузки на ESP32.

