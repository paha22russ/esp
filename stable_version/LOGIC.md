# Логика работы системы

## Общая архитектура

Система построена на принципах неблокирующего программирования для обеспечения отзывчивости веб-интерфейса и стабильной работы всех компонентов.

---

## 🔄 Основной цикл работы (loop)

### Приоритет выполнения задач

1. **OTA обновления** (высший приоритет)
   - `ArduinoOTA.handle()` - должен быть первым
   - Позволяет прервать выполнение для обновления

2. **Веб-сервер**
   - `server.handleClient()` - обработка HTTP запросов
   - Критично для отзывчивости интерфейса

3. **MQTT**
   - Проверка подключения каждые 10 секунд
   - Обработка входящих сообщений
   - Публикация данных

4. **Периодические задачи**
   - WiFi сканирование
   - Энкодер
   - Подброс угля
   - Обновление дисплея

---

## 🌡️ Логика управления температурой

### Уставка температуры (Setpoint)

**Диапазон**: 40-80°C  
**Шаг изменения**: 0.5°C  
**Источники изменения**:
1. Энкодер (поворот)
2. Веб-интерфейс (кнопки +/-)
3. MQTT команда (`{prefix}/setpoint/set`)

**Сохранение**:
- Отложенное сохранение в EEPROM (через 2 секунды после последнего изменения)
- Немедленное сохранение при изменении через веб-интерфейс настроек

**Публикация в MQTT**:
- При изменении энкодером → публикация в `{prefix}/setpoint`
- При изменении через веб → публикация в `{prefix}/setpoint`

---

## 🔥 Логика подброса угля

### Режим работы

**Активация**:
- Кнопка энкодера (переключатель)
- Кнопка в веб-интерфейсе (переключатель)
- MQTT команда (через API)

**Действия при активации**:
1. Сохраняется состояние вентилятора
2. Вентилятор выключается немедленно
3. Запускается таймер
4. Статус публикуется в MQTT

**Деактивация**:
- Повторное нажатие кнопки энкодера
- Кнопка в веб-интерфейсе
- Автоматически через 30 минут (защита)

**Управление вентилятором**:
- Во время подброса угля вентилятор принудительно выключен
- После деактивации управление возвращается автоматике

---

## 📡 Логика MQTT

### Подключение

**Параметры по умолчанию**:
- Server: `m5.wqtt.ru`
- Port: `5374`
- User: `u_OLTTB0`
- Password: `XDzCIXr0`
- Prefix: `kotel/device1`

**Процесс подключения**:
1. Генерация уникального Client ID на основе MAC адреса
2. Подключение с таймаутом 2 секунды (неблокирующее)
3. Подписка на `{prefix}/setpoint/set`
4. Автоматическое переподключение каждые 10 секунд при разрыве

### Публикация данных

**Простые топики** (каждые 10 секунд):
- `{prefix}/simple/temp` - температура подачи
- `{prefix}/simple/enabled` - состояние системы (1/0)
- `{prefix}/simple/setpoint` - уставка

**Полное состояние** (каждые 30 секунд):
- `{prefix}/state` - JSON со всеми данными

**События**:
- `{prefix}/coalFeeding` - статус подброса угля (1/0)
- `{prefix}/setpoint` - изменение уставки

### Получение команд

**Обрабатываемые топики**:
- `{prefix}/setpoint/set` - установка уставки (40-80°C)

---

## 📶 Логика WiFi

### Режимы работы

**1. Режим станции (STA)**
- Подключение к сохраненной WiFi сети
- IP адрес получается от DHCP
- OTA доступен по полученному IP

**2. Режим точки доступа (AP)**
- Создается сеть "KotelAP" (пароль: kotel12345)
- IP адрес: 192.168.4.1 (по умолчанию)
- OTA доступен по этому IP

### Процесс подключения

1. WiFiManager пытается подключиться к сохраненной сети
2. Таймаут: 180 секунд
3. При неудаче → создается точка доступа
4. При успехе → режим STA

### Сканирование сетей

**Асинхронное сканирование**:
1. Запрос через `/api/wifi/scan`
2. Запуск `WiFi.scanNetworks(true, true)` - асинхронный режим
3. Периодическая проверка результатов в `loop()`
4. Получение результатов через `/api/wifi/scan/results`

**Таймаут**: 10 секунд

---

## 💾 Логика сохранения данных

### EEPROM структура

```
Адрес 0:     Магическое число (0xAA)
Адрес 1-199: Автоматические настройки (JSON)
Адрес 200-399: MQTT настройки (JSON)
Адрес 400-599: Привязка датчиков (JSON)
Адрес 600:   Состояние системы (bool)
```

### Отложенное сохранение

**Принцип**: Сохранение выполняется через 2 секунды после последнего изменения.

**Применяется к**:
- Изменение уставки энкодером
- Изменение уставки через веб-интерфейс (кнопки +/-)

**Немедленное сохранение**:
- Изменение настроек через вкладки настроек
- Изменение состояния системы

**Преимущества**:
- Не блокирует веб-сервер при частых изменениях
- Снижает износ EEPROM
- Сохраняет все изменения (последнее значение)

---

## 🔄 Логика энкодера

### Обработка поворота

**Алгоритм**:
1. Прерывание на пинах CLK и DT (CHANGE)
2. Определение направления по переходам состояний
3. Изменение `encoderPosition` в ISR
4. В `loop()` вычисление разницы и изменение уставки

**Шаг**: 0.5°C на один щелчок энкодера

**Ограничения**: 40-80°C (автоматическая коррекция)

### Обработка кнопки

**Дебаунсинг**: 200мс между нажатиями

**Действие**: Переключение режима подброса угля

---

## 📺 Логика OLED дисплея

### Обновление

**Частота**: 1 раз в секунду

**Отображаемая информация**:
1. Температура подачи (крупно)
2. Уставка (средний шрифт)
3. Статус подброса угля (если активен)
4. WiFi статус/IP адрес

**Оптимизация**: Обновление только при необходимости (не каждый цикл).

---

## 🌐 Логика веб-сервера

### Обработка запросов

**Приоритет**: Высокий (обрабатывается сразу после OTA)

**Потоковая передача файлов**:
- HTML файл передается потоком из SPIFFS
- Не загружается полностью в память
- Работает с файлами любого размера

### API endpoints

Все endpoints возвращают JSON и обрабатываются быстро (неблокирующие операции).

**Обработка ошибок**:
- 404 для несуществующих путей
- 400 для неверных параметров
- Валидация всех входных данных

---

## ⚡ Оптимизация производительности

### Неблокирующие операции

1. **MQTT**:
   - Таймаут подключения: 2 секунды
   - Публикация без подтверждения (QoS 0)
   - Переподключение не чаще 1 раза в 10 секунд

2. **WiFi сканирование**:
   - Асинхронный режим
   - Таймаут 10 секунд

3. **EEPROM**:
   - Отложенное сохранение
   - Минимальное количество операций

4. **OLED**:
   - Обновление 1 раз в секунду
   - Не блокирует основной цикл

### Задержки

- Основной цикл: `delay(1)` - минимальная задержка
- Обновление дисплея: каждую секунду
- MQTT публикация: каждые 10-30 секунд
- Вывод в Serial: каждые 10 секунд

---

## 🔒 Безопасность

### Защита от ошибок

1. **Валидация данных**:
   - Все входные параметры проверяются
   - Диапазоны значений ограничены
   - Защита от переполнения

2. **Таймауты**:
   - Подброс угля: максимум 30 минут
   - WiFi сканирование: 10 секунд
   - MQTT подключение: 2 секунды

3. **Восстановление**:
   - Автоматическое переподключение MQTT
   - Защита от зависания (watchdog встроен в ESP32)

---

## 📊 Потоки данных

### Входные данные

1. **Энкодер** → изменение уставки
2. **Веб-интерфейс** → все настройки и управление
3. **MQTT** → команды управления
4. **Датчики температуры** → (TODO: реализация)

### Выходные данные

1. **OLED дисплей** → температура, уставка, статус
2. **Веб-интерфейс** → все данные через API
3. **MQTT** → публикация состояния
4. **Реле** → управление вентилятором и насосом

---

## 🔄 Состояния системы

### Переменные состояния

- `systemEnabled` - включена/выключена система
- `systemState` - текстовое состояние ("IDLE", "HEATING", etc.)
- `fanState` - состояние вентилятора
- `pumpState` - состояние насоса
- `coalFeedingActive` - активен ли подброс угля

### Переходы состояний

**Подброс угля**:
- Неактивен → Активен: вентилятор выключается
- Активен → Неактивен: вентилятор возвращается в автоматический режим

**Система**:
- Включена → Выключена: все реле выключаются
- Выключена → Включена: управление передается автоматике

---

## 🎯 TODO / Будущие улучшения

1. **Датчики температуры**:
   - Реальное сканирование DS18B20
   - Чтение температур с датчиков
   - Привязка к типам (подача, обратка, котельная, улица)

2. **Автоматика управления**:
   - Логика включения/выключения вентилятора
   - Логика включения/выключения насоса
   - Учет гистерезиса и инерции

3. **Дополнительные функции**:
   - NTP синхронизация времени
   - Логирование событий
   - Расширенная статистика

