# Стабильная версия прошивки ESP32 Котел v4.2.17

## 📋 Описание

Это стабильная версия прошивки для системы управления котлом на базе ESP32. Версия 4.2.17 включает все исправления и улучшения, протестированные в рабочем режиме.

## ✨ Основные возможности

- **Два режима работы:**
  - **Авто** - управление по температуре подачи с уставкой
  - **Комфорт** - управление по температуре дома с целевой температурой

- **Отслеживание датчика температуры дома:**
  - Проверка LWT статуса через MQTT (`home/esp01/status`)
  - Автоматическое переключение из режима Комфорт в Авто при offline датчика
  - Блокировка переключения в режим Комфорт при offline датчика

- **Веб-интерфейс:**
  - Адаптивный дизайн для портретной и альбомной ориентации
  - Отображение всех температур (подача, обратка, котельная, улица, дом)
  - Управление режимами работы
  - Настройки системы, MQTT, NTP, датчиков
  - OTA обновления через GitHub

- **MQTT интеграция:**
  - Публикация данных о температуре, состоянии системы
  - Подписка на команды управления
  - LWT (Last Will Testament) для отслеживания статуса

- **OLED дисплей:**
  - Отображение температуры подачи и уставки
  - Статус подброса угля или текущее время (если NTP включен)
  - WiFi статус

- **OTA обновления:**
  - Автоматическая загрузка прошивки и SPIFFS с GitHub
  - Прогресс обновления на OLED дисплее

## 📁 Структура проекта

```
stable_version_4.2.17/
├── src/
│   └── main.cpp          # Основной код прошивки
├── data/
│   └── index.html        # Веб-интерфейс
├── platformio.ini        # Конфигурация PlatformIO
├── upload_to_github.py   # Скрипт загрузки на GitHub
├── README.md             # Этот файл
├── ПИНЫ_ПОДКЛЮЧЕНИЯ.md  # Схема подключения
├── MQTT_TOPICS.md        # Описание MQTT топиков
└── README_PROJECT.md     # Общее описание проекта
```

## 🔧 Требования

- **PlatformIO** (рекомендуется) или Arduino IDE
- **ESP32 Dev Module** или совместимая плата
- **OLED дисплей** (128x64, I2C)
- **Датчики температуры** DS18B20 (OneWire)
- **Реле** для управления вентилятором и насосом
- **Энкодер** для управления (опционально)

## 📦 Установка и сборка

### 1. Установка PlatformIO

```bash
# Через pip
pip install platformio

# Или через установщик с https://platformio.org/
```

### 2. Клонирование/копирование проекта

Скопируйте папку `stable_version_4.2.17` в рабочую директорию.

### 3. Установка зависимостей

Зависимости автоматически установятся при первой сборке. Или вручную:

```bash
pio lib install
```

### 4. Сборка прошивки

```bash
# Сборка прошивки
pio run -e esp32dev_ota

# Сборка файловой системы (SPIFFS)
pio run -e esp32dev_ota -t buildfs
```

## 📤 Загрузка прошивки

### Загрузка через USB кабель

1. Подключите ESP32 к компьютеру через USB
2. Определите COM-порт (в Windows: Диспетчер устройств)
3. Отредактируйте `platformio.ini`, установив правильный `upload_port` (например, `COM3`)
4. Загрузите прошивку:

```bash
# Загрузка прошивки
pio run -e esp32dev_ota -t upload

# Загрузка файловой системы
pio run -e esp32dev_ota -t uploadfs
```

### Загрузка через OTA (Over-The-Air)

1. Убедитесь, что устройство подключено к WiFi
2. Используйте скрипт `upload_ota_final.py` или веб-интерфейс устройства

## 🌐 Первый запуск

1. После загрузки прошивки ESP32 создаст точку доступа WiFi
2. Подключитесь к сети `ESP32-Kotel` (пароль: `kotel12345`)
3. Откройте браузер и перейдите на `192.168.4.1`
4. Настройте WiFi подключение через веб-интерфейс
5. После подключения к WiFi устройство будет доступно по IP адресу или `kotel.local` (mDNS)

## ⚙️ Настройка

### Настройка через веб-интерфейс

1. Откройте веб-интерфейс устройства
2. Перейдите в раздел "Настройки"
3. Настройте:
   - **Система**: уставки, гистерезис, таймауты
   - **MQTT**: сервер, порт, логин, пароль, префикс топиков
   - **NTP**: сервер времени, часовой пояс
   - **Датчики**: привязка датчиков к параметрам

### Настройка MQTT

1. В настройках MQTT укажите:
   - Сервер MQTT брокера
   - Порт (обычно 1883 или 8883 для TLS)
   - Логин и пароль (если требуется)
   - Префикс топиков (например, `kotel/device1`)

2. Устройство автоматически подпишется на:
   - `{prefix}/setpoint/set` - установка уставки
   - `home/esp01/temperature` - температура дома
   - `home/esp01/status` - LWT статус датчика дома

3. Устройство публикует в:
   - `{prefix}/simple/supplyTemp` - температура подачи
   - `{prefix}/simple/returnTemp` - температура обратки
   - `{prefix}/simple/boilerTemp` - температура котельной
   - `{prefix}/simple/outdoorTemp` - температура улицы
   - `{prefix}/simple/homeTemp` - температура дома
   - `{prefix}/simple/workMode` - режим работы (0=Авто, 1=Комфорт)
   - `{prefix}/simple/targetHomeTemp` - целевая температура дома
   - `{prefix}/state` - полное состояние системы (JSON)

## 🔄 Обновление прошивки

### Обновление через веб-интерфейс

1. Откройте веб-интерфейс
2. Перейдите в "Настройки" → "Обновления"
3. Нажмите "Проверить обновления"
4. Если доступна новая версия, нажмите "Установить обновление"
5. Прогресс обновления отображается на OLED дисплее
6. Устройство автоматически перезагрузится после обновления

### Обновление через GitHub

1. Используйте скрипт `upload_to_github.py`:

```bash
python upload_to_github.py 4.2.17
```

2. Скрипт автоматически:
   - Скомпилирует прошивку и SPIFFS
   - Загрузит файлы на GitHub
   - Обновит версию

3. Устройство автоматически обнаружит обновление при следующей проверке

## 📊 Режимы работы

### Режим "Авто"

- Управление по температуре подачи
- Уставка задается вручную (обычно 60°C)
- Вентилятор включается при температуре ниже (уставка - гистерезис)
- Вентилятор выключается при температуре выше (уставка + гистерезис)

### Режим "Комфорт"

- Управление по температуре дома
- Целевая температура задается вручную (обычно 24°C)
- Автоматическое переключение состояний:
  - **WAIT** - ожидание начала нагрева
  - **HEATING** - активный нагрев
  - **MAINTAIN** - поддержание температуры
  - **COOLING** - охлаждение
- Требует наличия датчика температуры дома (LWT online)

## 🔌 Подключение оборудования

См. файл `ПИНЫ_ПОДКЛЮЧЕНИЯ.md` для подробной схемы подключения.

### Основные подключения:

- **OLED дисплей**: SDA → GPIO 21, SCL → GPIO 22
- **Датчики DS18B20**: Data → GPIO 4 (через подтягивающий резистор 4.7kΩ)
- **Реле вентилятора**: GPIO 26
- **Реле насоса**: GPIO 27
- **Реле датчиков**: GPIO 14
- **Энкодер**: CLK → GPIO 18, DT → GPIO 19, SW → GPIO 5

## 🐛 Отладка

### Serial Monitor

Подключите ESP32 к компьютеру и откройте Serial Monitor (115200 baud):

```bash
pio device monitor
```

### Диагностика через веб-интерфейс

1. Откройте веб-интерфейс
2. Перейдите в "Настройки" → "Диагностика"
3. Просмотрите информацию о системе, WiFi, MQTT

### API для диагностики

- `GET /api/diagnostics` - полная диагностическая информация
- `GET /api/status` - текущее состояние системы

## 📝 Известные проблемы и ограничения

- При первом запуске может потребоваться несколько секунд для инициализации WiFi
- MQTT подключение может занять до 10 секунд
- При обновлении через OTA веб-интерфейс может быть недоступен во время загрузки

## 🔐 Безопасность

- По умолчанию WiFi точка доступа защищена паролем `kotel12345`
- Рекомендуется изменить пароль OTA в коде перед использованием
- MQTT пароли хранятся в EEPROM (не в открытом виде)

## 📚 Дополнительная документация

- `README_PROJECT.md` - общее описание проекта
- `ПИНЫ_ПОДКЛЮЧЕНИЯ.md` - схема подключения оборудования
- `MQTT_TOPICS.md` - описание MQTT топиков

## 🔄 История версий

### v4.2.17 (Стабильная)

- Исправлена логика проверки датчика для режима Комфорт
- Улучшено отображение offline статуса датчика (серый цвет, без значка °C)
- Добавлено отображение времени на OLED дисплее (когда нет подброса угля)
- Убрано модальное окно прогресса обновления (не работало из-за блокировки веб-сервера)
- Исправлено отображение всех температур в веб-интерфейсе

### Предыдущие версии

См. историю изменений в основном проекте.

## 📞 Поддержка

При возникновении проблем:

1. Проверьте Serial Monitor для сообщений об ошибках
2. Проверьте веб-интерфейс диагностики
3. Убедитесь, что все подключения выполнены правильно
4. Проверьте настройки WiFi и MQTT

## 📄 Лицензия

Проект для личного использования.

---

**Версия:** 4.2.17  
**Дата:** 12.01.2026  
**Статус:** Стабильная версия