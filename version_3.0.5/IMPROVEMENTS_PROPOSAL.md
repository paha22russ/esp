# Предложения по улучшению безопасности и комфорта

## 🔴 Критичные улучшения (безопасность):

### 1. Защита от разгона без наддува
**Проблема:** Котел может разгоняться без вентилятора, что опасно.

**Решение:**
```cpp
// Отслеживать скорость роста температуры
float tempRiseRate = (currentTemp - lastTemp) / timeDelta; // °C/мин
if (tempRiseRate > 2.0 && !fanState) {
    // Принудительно включить вентилятор
    fanState = true;
    systemState = "SAFETY_FAN";
    Serial.println("[Безопасность] Быстрый рост температуры - включен вентилятор");
}
```

**Параметры:**
- Порог скорости роста: 2.0°C/мин (настраиваемый)
- Время анализа: последние 2-3 минуты

---

### 2. Защита от закипания
**Проблема:** Большая разница температур подача-обратка может указывать на закипание.

**Решение:**
```cpp
float tempDiff = supplyTemp - returnTemp;
if (tempDiff > 20.0 && pumpState) {
    // Большая разница - возможное закипание
    systemState = "BOILING_RISK";
    // Можно выключить насос или включить вентилятор на максимум
    Serial.println("[Безопасность] Большая разница температур - риск закипания");
}
```

**Параметры:**
- Максимальная разница: 20°C (настраиваемый)
- Действие: предупреждение + возможно выключение насоса

---

### 3. Использование heatingTimeout
**Проблема:** Если котел долго не выходит на уставку - возможно проблема.

**Решение:**
```cpp
unsigned long heatingStartTime = 0;
if (fanState && heatingStartTime == 0) {
    heatingStartTime = millis();
}
if (heatingStartTime > 0 && millis() - heatingStartTime > autoSettings.heatingTimeout * 60000) {
    if (supplyTemp < autoSettings.setpoint - 5) {
        // Долгий разогрев без результата
        systemState = "HEATING_TIMEOUT";
        Serial.println("[Предупреждение] Долгий разогрев - возможно проблема");
        // Можно выключить вентилятор для проверки
    }
}
```

---

## 🟡 Важные улучшения (комфорт):

### 4. Использование инерции системы
**Проблема:** После выключения вентилятора насос сразу выключается, но тепло еще есть.

**Решение:**
```cpp
unsigned long inertiaStartTime = 0;
if (!fanState && fanStateWasOn) {
    inertiaStartTime = millis();
    fanStateWasOn = false;
}
if (inertiaStartTime > 0) {
    unsigned long inertiaElapsed = (millis() - inertiaStartTime) / 60000; // минуты
    if (supplyTemp > autoSettings.inertiaTemp && 
        inertiaElapsed < autoSettings.inertiaTime) {
        // Продолжаем циркуляцию
        pumpState = true;
    } else {
        inertiaStartTime = 0;
    }
}
```

---

### 5. Обнаружение прогорания угля
**Проблема:** Когда уголь прогорел, температура падает даже с вентилятором.

**Решение:**
```cpp
// Анализ тренда при работающем вентиляторе
if (fanState && supplyTemp > 0) {
    int trend = getTemperatureTrend(&supplyHistory);
    static unsigned long trendCheckStart = 0;
    
    if (trend == -1) { // Падение температуры
        if (trendCheckStart == 0) {
            trendCheckStart = millis();
        }
        if (millis() - trendCheckStart > 10 * 60000) { // 10 минут падения
            systemState = "COAL_BURNED";
            Serial.println("[Предупреждение] Возможно уголь прогорел - температура падает");
        }
    } else {
        trendCheckStart = 0;
    }
}
```

---

### 6. Предупреждение о низкой температуре обратки
**Проблема:** Холодная обратка может указывать на проблемы.

**Решение:**
```cpp
if (pumpState && returnTemp > 0 && returnTemp < 40.0) {
    systemState = "LOW_RETURN_TEMP";
    Serial.println("[Предупреждение] Низкая температура обратки");
}
```

---

## 🟢 Желательные улучшения (удобство):

### 7. Умный старт
**Проблема:** При холодном старте резкое включение вентилятора может быть неэффективно.

**Решение:**
```cpp
if (supplyTemp < 30.0 && !fanState) {
    // Холодный старт - сначала насос, потом вентилятор
    pumpState = true;
    delay(60000); // 1 минута циркуляции
    fanState = true;
}
```

---

### 8. Использование maxTemp
**Проблема:** Параметр есть, но не используется.

**Решение:**
```cpp
if (supplyTemp >= autoSettings.maxTemp && supplyTemp < autoSettings.overheatTemp) {
    systemState = "HIGH_TEMP";
    Serial.println("[Предупреждение] Высокая температура - близко к максимуму");
}
```

---

### 9. Защита от застоя
**Проблема:** Насос может выключаться слишком быстро.

**Решение:**
```cpp
unsigned long pumpStartTime = 0;
if (pumpState && pumpStartTime == 0) {
    pumpStartTime = millis();
}
if (pumpStartTime > 0 && millis() - pumpStartTime < 60000) {
    // Минимум 1 минута работы насоса
    pumpState = true;
}
```

---

## Новые состояния системы:

- `SAFETY_FAN` - вентилятор включен по безопасности
- `BOILING_RISK` - риск закипания
- `HEATING_TIMEOUT` - долгий разогрев
- `COAL_BURNED` - возможно уголь прогорел
- `LOW_RETURN_TEMP` - низкая температура обратки
- `HIGH_TEMP` - высокая температура (предупреждение)

## Приоритеты внедрения:

1. **Защита от разгона** - критично для безопасности
2. **Защита от закипания** - критично для безопасности
3. **Использование инерции** - важно для комфорта
4. **Обнаружение прогорания** - важно для комфорта
5. **Остальные** - по желанию
