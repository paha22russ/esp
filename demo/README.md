# Demo UI (offline)

Открой `boiler-ui-demo.html` в браузере (двойной клик или через локальный сервер).

Это копия веб-интерфейса котла с **фейковым API**: значения температур, Wi‑Fi, MQTT, настройки и кнопки работают без ESP32.

Оранжевая полоска сверху означает DEMO-режим.
